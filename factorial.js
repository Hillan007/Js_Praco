function factorial(a,arr){
    arr.forEach(number=>{
        console.log( `${a} * ${number} = ${number*a}`)
    })
}
let max=input("Enter the maximum number for the factorials: ")
/*let arr=Range(1,max)

factorial(2,arr)*/